Функция ЕстьСкидкаОкруглениемСтроки(ДокументОбъект)
	Результат = Ложь;
	Если ДокументОбъект.СкидкиНаценки.Количество() > 0 Тогда
	
		СкидкиНаценки = ДокументОбъект.СкидкиНаценки.ВыгрузитьКолонку("СкидкаНаценка");
		ДанныеСкидок = ОбщегоНазначения.ЗначениеРеквизитаОбъектов(СкидкиНаценки, "СпособПредоставления");
		
		СпособОкругленияПоСтроке = Перечисления.СпособыПредоставленияСкидокНаценок.RM_ОкруглениеСуммыДляКаждойСтроки;
		Для каждого СК из ДанныеСкидок  Цикл   
		
			Если СК.Значение = СпособОкругленияПоСтроке Тогда
				Результат = Истина;
				Прервать;
			КонецЕсли;
		
		КонецЦикла;
		
			
	
	КонецЕсли;      
	
	Возврат Результат;
КонецФункции
